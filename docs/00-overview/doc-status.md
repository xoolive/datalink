# Documentation freshness

Use this rule before adopting external guidance:

- `current`: recently verified against current code/tools
- `needs-review`: likely useful, but not recently validated
- `historical`: context only; expect drift

## External source status (initial)

- `dumpvdl2` README/code: `current`
- `libacars` README/API docs: `current`
- `vdlm2dec` README: `needs-review`
- `acarsdec` README/code: `needs-review`
- `dumphfdl` README/code: `current`
- `thebaldgeek.github.io` pages: mixed, many are `historical` or `needs-review`

When adding docs, include source path and date reviewed.
